// Jest setup file — runs before each test suite.
//
// Imports @testing-library/jest-dom which adds custom matchers like:
//   - toBeInTheDocument()
//   - toHaveTextContent()
//   - toBeVisible()
//   - toBeDisabled()
//
// These matchers make test assertions more readable and expressive
// compared to checking raw DOM properties.

import "@testing-library/jest-dom";
