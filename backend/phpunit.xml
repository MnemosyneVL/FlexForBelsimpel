<?xml version="1.0" encoding="UTF-8"?>
<!-- PHPUnit configuration for running backend tests.
     Run with: php artisan test (or docker compose exec php php artisan test) -->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         cacheDirectory=".phpunit.cache"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <!-- Use array cache in tests so tests don't depend on Redis -->
        <env name="CACHE_STORE" value="array"/>
        <!-- Use sync queue in tests so jobs run immediately -->
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="DB_DATABASE" value="flexforbelsimpel_test"/>
    </php>
</phpunit>
